/* const fetchPrueba = () => {
  return fetch("https://jsonplaceholder.typicode.com/posts").then((response) =>
    response.json()
  );
};


export default async function Prueba() {
  const pruebas = await fetchPrueba();

  return (
    <div>
      {pruebas.map((prueba) => (
        <ul key={prueba.id}>
          <li>{prueba.id}</li>
          <li>{prueba.title}</li>
          <li>{prueba.body}</li>
        </ul>
      ))}
    </div>
  );
}
*/